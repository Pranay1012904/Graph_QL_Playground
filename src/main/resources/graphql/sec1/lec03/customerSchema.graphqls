type Query{
    customers:[Customer]
}

type Customer{#use this to fetch orders in the object
    id: ID
    name: String
    age: Int
    city: String
    order:[CustomerOrder]
}

type CustomerOrder{
    id: ID
    description:String
}